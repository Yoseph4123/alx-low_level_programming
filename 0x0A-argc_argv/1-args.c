#include <stdio.h>
/**
  *main - entry point
  *@avgc: is an integer
  *@avgv: is a string
  *Return: always 0 (sucess)
  */
int main(int avgc, char *avgv[])
{
	printf("%d\n", avgc - 1);
	return (0);
}
