#include <stdio.h>
#include "main.h"
#include <string.h>

/**
  *_strcmp - simpares two strings
  *@s1: is a pointer
  *@s2: is a pointer
  *Return: a
  */

int _strcmp(char *s1, char *s2)
{
	int a;

	a = strcmp(s1, s2);
	return (a);
}
